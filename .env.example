# Copy to .env locally. Never commit real values.

APP_ENV=local
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/zen_ledger_v2

# Add API keys only when the relevant integration is implemented.
OPENAI_API_KEY=
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

