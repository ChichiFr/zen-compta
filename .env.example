# Copy to .env locally. Never commit real values.

ENVIRONMENT=local
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/zen_compta
INTERNAL_API_TOKEN=change-me-in-local-env

# Add API keys only when the relevant integration is implemented.
OPENAI_API_KEY=
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox
